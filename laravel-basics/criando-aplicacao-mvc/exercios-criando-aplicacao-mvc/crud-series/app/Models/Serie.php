<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

// Eloquent ORM vai procurar a tabela "series", plural do nosso model aqui == Serie, então não precisamode um protected $table = 'series' por ex, nesse caso, apenas essa estrutura aqui é o suficiente
class Serie extends Model
{
    //
}
